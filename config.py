# this module is used to read configuration options from file
# the long-term goal is that a config file would contain only username and password
# the short-term goal is that it will read authkey needed for signing requests
